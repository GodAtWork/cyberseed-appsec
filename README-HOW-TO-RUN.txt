
EXECUTING:

  This project compiles as a self executing jar you may run them as follows:

./sageServer.jar

OR

java -jar sageServer.jar


Also we have included run.sh which will attempt to find the jar and execute it for you.
